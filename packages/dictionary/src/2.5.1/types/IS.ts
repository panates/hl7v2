import { DeepPartial } from 'ts-gems';
import { HL7DataTypeDefinition } from '../../types.js';

export const IS: DeepPartial<HL7DataTypeDefinition> = {
  desc: 'Coded value for user-defined tables',
};
